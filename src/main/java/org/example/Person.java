package org.example;

public class Person {

    /*
    First, create a Person class with integer age and string phoneNumber fields.

    For example, code similar to this should be possible:

    ListSumCalculator<Person> calc = new ListSumCalculator<>(listOfPeople);
    calc.sumBy(p -> p.age); // in this case we want to sum by age
    calc.sumBy(p -> Integer.parseInt(p.phoneNumber)); // sum by phone number
    calc.sumBy(p -> Integer.parseInt(p.phoneNumber) + p.age); // sum all the ages plus all the phone numbers in the list
     */
    int age;
    String phoneNumber;

}
